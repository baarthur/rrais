#' List of firm dataset variables
#'
#' For each variable, we list its coverage period, eventual years skipped, the original name (`alias`) and the
#'  standard variable name that we adopted (`new_name`). Note that `new_name` repeats because data layout changes
#'  throughout the years.
#'
#' @author Arthur Alvarenga \url{baarthur.github.io}

"dic_firms"
